name: Shellcheck

on:
  push:
    paths: [ '**.sh' ]
  pull_request:
    paths: [ '**.sh' ]

jobs:
  Shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run shellcheck
        run:  find . -type f -iname "*.sh" -exec shellcheck {} \;
